# Context

<!-- What this project IS. Not how to work in it — that is AGENTS.md, which
     is loaded on every request and has to stay short. This one is read on
     demand, which is what lets it be as long as it is useful.

     Delete every placeholder you do not fill in. An unanswered prompt left
     in the file reads as an answer. -->

## What this is

<One paragraph. What does this project do, and for whom? Someone who has
never seen it should be able to decide from this whether they are in the
right repository.>

## Why it exists

<The problem. What was happening before this existed, and what it cost.>

## Shape

<The three or four things a newcomer must hold in their head. Not every
module — the ones that, if you misunderstand them, everything else you read
will be wrong.>

## Vocabulary

<Words this project uses in a way the reader would not guess. Every codebase
has three or four, and they are the cheapest thing to write down and the most
expensive to leave out.>

| term | means here |
|---|---|
| <word> | <what it means in this repository, specifically> |

## Constraints that are not negotiable

<Things that look arbitrary and are not: a platform that must be supported, a
dependency that cannot be added, a performance floor, a compliance rule. Each
with the reason, because a constraint without a reason gets "simplified" by
the next person who reads it.>

## What this is not

<Scope boundaries. The features people ask for that this project deliberately
does not have, and why.>
