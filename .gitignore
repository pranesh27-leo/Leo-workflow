.leo/plan.md
.leo/manifest.md
.leo/session
.leo/tasks/
dist/
t/fixtures/*/
# ...except the sample task files, which are inputs to the benchmark rather
# than output from it. Generated fixtures are derived from git history; these
# are not derivable from anything, and losing them loses a comparison.
!t/fixtures/tasks/
.leo/commits/
.leo/used
.leo/current
SESSION.md

# npm pack / npm publish build artefacts. Regenerated on demand; a
# 127KB tarball in git history is a tarball forever.
*.tgz
