# CLAUDE.md

The working rules for this repo live in `AGENTS.md`, which every coding agent
reads. What the repo *is* lives in `CONTEXT.md`. Do not duplicate either here —
two copies always drift.

@AGENTS.md
