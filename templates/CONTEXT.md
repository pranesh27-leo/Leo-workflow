# <Project name>

<!--
What this repository IS. AGENTS.md says how to work; this says what you are
working on. Split on purpose: AGENTS.md loads on every request of every
session, so every line in it is paid for forever, and a long one gets skimmed
by exactly the models that most need to read it.

This file is read on demand, so it can be as long as it is useful.
-->

## What this is

<One paragraph: what the system does, who depends on it, what breaks if it is
wrong.>

## Shape of the code

<Where the important things live. Not a directory listing -- the source tree
already is one. The two or three paths someone would otherwise waste an hour
finding.>

## Non-obvious rules

<!-- Each line should be something that would cause a wrong-but-plausible
     change if the agent did not know it. Delete this section if nothing does. -->

- Never edit files under `<path>` — they are generated by `<cmd>`.
- `<module>` is load-bearing for `<thing>`; changes there need `<extra check>`.

## Vocabulary

<Words this project uses in a way an outsider would get wrong. A grill that
settles a term belongs here, so the next session starts where this one ended.>

## Decisions that are already made

<Things settled earlier that should not be reopened without a reason, and the
reason each was settled. This is what stops an agent helpfully rediscovering
an option you rejected two months ago.>
