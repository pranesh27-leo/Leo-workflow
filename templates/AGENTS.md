# <Project name>

<!--
Read by Codex, Cursor, Copilot, Gemini CLI, Aider, Windsurf, Zed and others.
Claude Code reads CLAUDE.md, which imports this file.

KEEP IT UNDER 50 LINES. It loads on every session, so every line costs you on
every request. Include only what an agent would get WRONG by reading the code.
Delete anything the source tree already says.
-->

## What this is

<One or two sentences: what the system does, and who depends on it.>

## Commands

- Build: `<cmd>`
- Test:  `<cmd>`
- Run:   `<cmd>`

## Non-obvious rules

<!-- Each line should be something that would cause a wrong-but-plausible
     change if the agent did not know it. Delete this section if nothing does. -->

- Never edit files under `<path>` — they are generated by `<cmd>`.
- `<module>` is load-bearing for `<thing>`; changes there need `<extra check>`.

## Conventions

- Commit subjects: `<module>: <what changed>`
- One task per commit. Never batch unrelated changes.

## How we work

Before planning, building, reviewing or committing a change, read
`.leo/workflow.md` and follow the matching step. Do only what was asked.
