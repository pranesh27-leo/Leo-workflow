---
name: grill-me
description: A relentless interview to sharpen a plan or design. Invoke before every task and every subtask.
disable-model-invocation: true
---

Read `.leo/skills/grilling/SKILL.md` and follow it exactly. Invent nothing.

That file is the definition of a grill. It is vendored unchanged, so the
definition cannot drift between two sessions or two repositories.

If your runtime has a skill mechanism of its own, this entry is what it
invokes, and the line above is all it needs to do. If it does not, the path
is still the answer: open the file and follow it.
